// A stand-in for a compiled bundle that smuggled an arbitrary value through.
// Shaped like real Vite output: the class survives minification as a plain
// string literal, which is exactly what both scanners read.
const b = "flex items-center gap-2 p-4 w-[137px]";
const c = "[&>tr]:border-border";
export { b, c };
