# `@omadia/plugin-api` is a PRIVATE workspace package inside byte5ai/omadia — it
# lives on no registry. The Omadia host provides it at runtime, so this repo
# declares it as a peer and links it from a sibling checkout (see README.md).
# Without this flag npm's peer auto-install 404s trying to fetch it.
legacy-peer-deps=true
