# Overlay: the dev platform (epic #470) — isolated per-job runner containers.
#
#   docker compose \
#     -f /path/to/omadia/docker-compose.yaml \
#     -f /path/to/omadia-dev-platform/docker-compose.dev-platform.yaml up -d
#
# The overlay lives in THIS repo, beside the sidecars it builds (epic #470 P4).
# Compose resolves each service's `build.context` relative to the file that
# declares it, so `context: sidecars/…` below resolves here even when the base
# `docker-compose.yaml` comes from an omadia core checkout somewhere else.
# In production nothing is built at all — the images come from GHCR, signed
# (see docs/SUPPLY_CHAIN.md).
#
# The whole point of this file is a property the middleware cannot enforce about
# itself: THE MIDDLEWARE NEVER TOUCHES A DOCKER SOCKET. It asks a small daemon to
# create a container; that daemon talks to a dedicated, nested docker engine
# (`dev-dind`) that holds nothing else. If the middleware is compromised — the
# threat this design assumes — the blast radius is one dind, on internal-only
# networks, whose containers can only reach the world through a default-deny
# proxy that authorises them per job.
#
# Four services, four networks, and the reasons they are shaped this way:
#
#   dev-runner-daemon  the ONLY holder of docker credentials. Sits on dev-control
#                      (to be called by the middleware, and to call it back) and
#                      dev-engine (to reach dind). NOT on `omadia`: nothing else in
#                      the stack may reach its control API.
#   dev-dind           the nested engine. `privileged: true` — the only service in
#                      the whole compose stack that is. Therefore it sits on
#                      internal-only networks, publishes no host port, and speaks
#                      TLS only (the daemon refuses a plaintext 2375 engine).
#   dev-egress-proxy   the ONLY path from a job container to the internet.
#                      Default-deny; the daemon registers each job's allowlist and
#                      a per-job credential before its container starts.
#   middleware         joins dev-control so it can call the daemon. It gains NO
#                      docker socket, NO DOCKER_HOST, and no route to dev-engine.
#
# The proxy has a PINNED IP (172.28.5.3) rather than a DNS name because job
# containers are created by the nested dind, not by compose — they never see
# compose's DNS. `HTTP_PROXY` inside a runner must therefore be an address.
#
# Secrets: every value below is a placeholder read from the environment. Generate
# DEV_RUNNER_DAEMON_TOKEN with `openssl rand -hex 32`; it is a comma-separated list
# so it can be rotated with zero downtime (both ends ACCEPT every token, senders
# SEND the first). Nothing here belongs in git.

services:
  # --- the middleware gains a client, and nothing else ----------------------
  middleware:
    environment:
      DEV_PLATFORM_ENABLED: 'true'
      DEV_PLATFORM_BACKEND: docker
      # The daemon's control API, reachable only across dev-control.
      DEV_RUNNER_DAEMON_URL: http://172.28.4.2:7411
      DEV_RUNNER_DAEMON_TOKEN: ${DEV_RUNNER_DAEMON_TOKEN:?set DEV_RUNNER_DAEMON_TOKEN}
      # Where the runner phones home. The daemon injects this into every job; the
      # runner reaches it across dev-control, never over the public internet.
      DEV_PLATFORM_RUNNER_BASE_URL: http://middleware:8080
      # The image the middleware derives into every job's policy (served over
      # GET /internal/job-policy/:jobId, which the daemon fetches at provision
      # time). Without this, jobPolicyConfig never builds and that endpoint
      # 503s forever — every DockerBackend provision fails at the first real
      # container (i.e. the implement phase; analyze/plan/clarify run without
      # one). Same source var as the daemon's own DEV_RUNNER_IMAGES below, so
      # both sides always agree on which image a job runs.
      DEV_RUNNER_DEFAULT_IMAGE: ${DEV_RUNNER_IMAGE:-ghcr.io/byte5ai/omadia-dev-platform-runner:latest}
      # The LLM proxy's model allowlist (spec §5, wireDevPlatform.ts). An empty
      # list is a deliberate fail-closed default (config.ts: "always mounted;
      # an empty allowlist ⇒ it answers 500 'no LLM policy'"), so this is a
      # real per-deployment setting, not a wiring bug -- but its absence looks
      # EXACTLY like every earlier gate in this chain from the runner's side:
      # the CLI reaches the proxy fine (gates 6/10 fixed that) and gets an
      # instant, silent-to-the-runner 500 with zero tokens spent. Comma-separated,
      # exact string match (llmProxy.ts: `policy.allowedModels.includes(model)`)
      # against whatever `--model` the CLI actually reports in its init message.
      DEV_PLATFORM_LLM_ALLOWED_MODELS: ${DEV_PLATFORM_LLM_ALLOWED_MODELS:-claude-opus-4-8[1m],claude-opus-4-8,claude-sonnet-4-8[1m],claude-sonnet-4-8}
      # Egress allowlist entries every job gets in ADDITION to the middleware
      # host + its own repo's forge host (deriveJobPolicy.ts). Absent by
      # default (config.ts), which is correct for a repo needing no package
      # install at all -- but for THIS repo (npm workspaces), a job with no
      # bootstrap_command auto-detects `npm ci`/`npm install` (bootstrapDetect
      # .ts) and, same as any implement-phase agent legitimately running one
      # itself, needs a route to the registry or it just hangs retrying
      # against a proxy default-deny (found live: "install is stalled,
      # node_modules not growing", no clean error -- npm's own resilience
      # masks the denial as a hang rather than a fast rejection).
      DEV_EGRESS_BASE_ALLOWLIST: ${DEV_EGRESS_BASE_ALLOWLIST:-registry.npmjs.org}
      # Neutralise any docker engine address a stray `middleware/.env` (loaded via
      # the base file's env_file) might inject. `environment` wins over env_file,
      # so these empty values are the last word: the middleware CANNOT be handed a
      # socket through the back door. Asserted on the MERGED config, not just here.
      DOCKER_HOST: ''
      DOCKER_TLS_VERIFY: ''
      DOCKER_CERT_PATH: ''
    networks: [omadia, dev-control]
    # No `volumes:` entry for /var/run/docker.sock, and no DOCKER_HOST. Both are
    # asserted absent by test/devplatform-docker/composeTopology.test.ts — the
    # single most important property of this file.
    depends_on:
      dev-runner-daemon:
        condition: service_healthy

  # --- the only holder of docker credentials --------------------------------
  dev-runner-daemon:
    build:
      context: sidecars/dev-runner-daemon
    image: omadia-dev-runner-daemon:local
    restart: unless-stopped
    command: ['node', 'src/daemon.mjs']
    environment:
      # Bind to the dev-control interface ONLY, never a wildcard: the daemon also
      # sits on dev-engine, and a wildcard bind would expose its control API to
      # every container the nested engine runs. `assertControlPlaneBind` refuses
      # 0.0.0.0, so the address is pinned (a container's IP is otherwise dynamic).
      DEV_DAEMON_BIND: '172.28.4.2'
      DEV_DAEMON_PORT: '7411'
      DEV_RUNNER_DAEMON_TOKEN: ${DEV_RUNNER_DAEMON_TOKEN:?set DEV_RUNNER_DAEMON_TOKEN}

      # The nested engine. TLS is mandatory — the daemon refuses tcp://…:2375.
      # Addressed by dind's PINNED dev-engine IP (172.28.6.2, fixed in the
      # `dev-engine` network below), NOT by the `dev-dind` name: dind's server cert
      # carries its IPs, `docker`, `localhost` — never `dev-dind` — so a name-based
      # DOCKER_HOST fails --tlsverify's hostname check. dind regenerates that server
      # cert from its ACTUAL IPs on every boot (only /certs/client is persisted;
      # /certs/{ca,server} are ephemeral), so a pinned IP is always in the fresh
      # cert and survives a recreate — unlike a docker-pool-assigned IP, which a
      # recreate can move out from under the cert.
      DOCKER_HOST: tcp://172.28.6.2:2376
      DOCKER_TLS_VERIFY: '1'
      DOCKER_CERT_PATH: /certs/client

      # Policy source. The daemon fetches each job's policy ITSELF; it never takes
      # one from its caller.
      OMADIA_INTERNAL_API_URL: http://middleware:8080

      # The image allowlist is the boundary a compromised middleware cannot cross:
      # it may name a job, never an image. The daemon REFUSES TO BOOT without it.
      DEV_RUNNER_ALLOWED_IMAGES: ${DEV_RUNNER_ALLOWED_IMAGES:-ghcr.io/byte5ai/omadia-dev-platform-runner}
      DEV_RUNNER_IMAGES: ${DEV_RUNNER_IMAGE:-ghcr.io/byte5ai/omadia-dev-platform-runner:latest}
      # Digest-pinned by default (true) — the comment above used to be the ONLY
      # place this knob existed; the var was never actually forwarded into the
      # container, so `env.DEV_RUNNER_REQUIRE_DIGEST` was always undefined and
      # `parseRequireDigest` silently fell back to true. Every locally-built
      # image is a floating tag (no digest, no registry to have digest-pinned
      # it from), so EVERY provision was refused with the same generic
      # "the middleware could not supply the job policy" 502 the runner-image
      # gap produced — a second, distinct cause behind the identical symptom.
      # Set DEV_RUNNER_REQUIRE_DIGEST=0 in .env to relax this, locally only.
      DEV_RUNNER_REQUIRE_DIGEST: ${DEV_RUNNER_REQUIRE_DIGEST:-true}

      # Pull policy. `always` (the default, and prod on GHCR) re-pulls + digest-pins
      # + cosign-verifies every image on every provision. `if-not-present` skips the
      # pull ONLY when the image is already cached in dind — for local dev, where the
      # runner image is built + `docker load`ed straight into dind and the
      # default-deny egress proxy would answer a registry pull with 407. It changes
      # NOTHING about the image allowlist (DEV_RUNNER_ALLOWED_IMAGES) or the digest
      # requirement — both are enforced against the job policy before any pull.
      DEV_RUNNER_PULL_POLICY: ${DEV_RUNNER_PULL_POLICY:-if-not-present}

      # Egress. These two are configured TOGETHER or not at all: a job routed
      # through the proxy that the daemon cannot register with it is answered 407
      # on every request, which inside the runner looks like a broken network.
      # The daemon refuses to boot on a half-configuration.
      DEV_RUNNER_EGRESS_PROXY_URL: http://172.28.5.3:3128
      DEV_RUNNER_EGRESS_PROXY_CONTROL_URL: http://172.28.4.3:3129
      # The runner does NOT reach the middleware directly -- it CANNOT: job
      # containers are created by dind on their own per-job network, which has
      # no route to dev-control (where `middleware` actually lives). Only the
      # proxy is dual-homed onto both dev-egress (job-reachable) and dev-control
      # (middleware-reachable), so phone-home traffic must go THROUGH it, not
      # around it. The proxy's own egress policy (egressPolicy.mjs) already
      # special-cases exactly this: a request whose host+port equals
      # OMADIA_INTERNAL_API_URL (this same dev-egress-proxy's own env, set to
      # http://middleware:8080) is allowed regardless of path -- so listing
      # "middleware" here to bypass the proxy doesn't skip an unnecessary hop,
      # it routes phone-home into a dead end: `getaddrinfo ENOTFOUND middleware`
      # from inside the job's network, which is where every real job died after
      # the runner-image/digest/token gates were fixed. Only localhost/127.0.0.1
      # (traffic that never leaves the container) belong on this bypass list.
      DEV_RUNNER_NO_PROXY: localhost,127.0.0.1

      # A lease says "still working"; it can never say "run forever".
      DEV_RUNNER_MAX_JOB_LIFETIME_MS: ${DEV_RUNNER_MAX_JOB_LIFETIME_MS:-21600000}
    volumes:
      - dev-dind-certs:/certs/client:ro
    networks:
      dev-control:
        ipv4_address: 172.28.4.2
      # Pinned so the daemon's dev-engine address is deterministic across recreates.
      dev-engine:
        ipv4_address: 172.28.6.3
    # No `ports:` — the daemon is unreachable from the host and from `omadia`.
    depends_on:
      dev-dind:
        condition: service_healthy
      dev-egress-proxy:
        condition: service_started
    healthcheck:
      test: ['CMD', 'node', '-e', "fetch('http://172.28.4.2:7411/v1/health',{headers:{authorization:'Bearer '+process.env.DEV_RUNNER_DAEMON_TOKEN.split(',')[0]}}).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  # --- the nested engine: the only privileged service in the stack ----------
  dev-dind:
    # Thin wrapper around docker:27-dind (see sidecars/dev-dind):
    # adds ONE static iptables rule in dind's OWN netns so a nested per-job
    # container's direct-connect bypass attempt (confirmed live, 2026-07-29 —
    # npm's own proxy resolution occasionally lands on a direct-connect code
    # path) fails in milliseconds instead of a multi-minute TCP blackhole.
    # dev-engine/dev-egress are already `internal: true`, so the bypass was
    # ALWAYS doomed — this only makes the failure deterministic, closing the
    # timing window that re-triggers npm's own ExitHandler race (npm/cli#9751).
    # Zero capability change to the job container itself.
    image: omadia-dev-dind:local
    build:
      context: sidecars/dev-dind
    restart: unless-stopped
    privileged: true
    environment:
      DOCKER_TLS_CERTDIR: /certs
      # Even the nested engine's own registry pulls traverse the egress proxy, so
      # a compromised daemon cannot reach an arbitrary registry.
      HTTP_PROXY: http://172.28.5.3:3128
      HTTPS_PROXY: http://172.28.5.3:3128
      NO_PROXY: dev-runner-daemon,middleware,localhost,127.0.0.1
    volumes:
      - dev-dind-certs:/certs/client
      - dev-dind-data:/var/lib/docker
    # dev-engine reaches the daemon; dev-egress carries the job containers' traffic
    # to the proxy. Both are `internal: true`, so this privileged container has no
    # route to the host network and no route out except through the proxy.
    #
    # Both IPs are PINNED. The daemon's DOCKER_HOST is dind's dev-engine address, and
    # dind bakes its actual IPs into the TLS server cert it regenerates on every boot
    # — pinning keeps the cert's SAN and the daemon's target the same address forever,
    # so a recreate can never move the IP out from under --tlsverify.
    networks:
      dev-engine:
        ipv4_address: 172.28.6.2
      dev-egress:
        ipv4_address: 172.28.5.2
    # No `ports:` — nothing outside dev-engine may speak to a privileged docker API.
    healthcheck:
      test: ['CMD', 'docker', 'info']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  # --- the only path from a job container to the world ----------------------
  dev-egress-proxy:
    # SAME image as the daemon, different entrypoint: one build, two services, so
    # the proxy never inherits the daemon's docker credentials.
    build:
      context: sidecars/dev-runner-daemon
    image: omadia-dev-runner-daemon:local
    restart: unless-stopped
    command: ['node', 'src/proxy.mjs']
    environment:
      DEV_RUNNER_DAEMON_TOKEN: ${DEV_RUNNER_DAEMON_TOKEN:?set DEV_RUNNER_DAEMON_TOKEN}
      DEV_EGRESS_DATA_PORT: '3128'
      DEV_EGRESS_CONTROL_PORT: '3129'
      # The one deliberate internal destination: the middleware's LLM proxy. Every
      # other internal address is refused, including an allowlisted name that
      # resolves to one (the rebinding defence).
      OMADIA_INTERNAL_API_URL: http://middleware:8080
    networks:
      dev-egress:
        # Pinned: job containers are created by dind, not compose, so they cannot
        # resolve `dev-egress-proxy`. Their HTTP_PROXY must be an address.
        ipv4_address: 172.28.5.3
      dev-control:
        # The daemon reaches the CONTROL plane here (dev-control); job containers
        # reach the DATA plane at 172.28.5.3 (dev-egress). Two planes, two networks,
        # and the daemon never joins the one the jobs are on.
        ipv4_address: 172.28.4.3
      # The proxy's OWN route to the real internet. dev-egress and dev-control
      # are BOTH `internal: true` -- correctly, they must never reach outside --
      # but that left the proxy itself with no path out either, so every job's
      # egress request failed DNS resolution before the allowlist/CONNECT logic
      # ever ran (`getaddrinfo EAI_AGAIN github.com` from inside this very
      # container). The proxy deliberately does NOT join `omadia` for this --
      # sharing the app's own network would make it reachable from (and able to
      # reach) middleware/web-ui laterally, which the whole point of a separate
      # egress plane exists to avoid -- so this is a THIRD, dedicated network
      # whose only member is the proxy.
      dev-egress-external: {}
    # No `ports:` — the proxy is not reachable from the host.

volumes:
  dev-dind-certs:
  dev-dind-data:

networks:
  # The middleware ↔ daemon ↔ middleware control path. Internal: nothing here is
  # reachable from the host.
  dev-control:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 172.28.4.0/24
  # The daemon ↔ dind engine path. Nothing else joins it. The subnet is PINNED so
  # dind's dev-engine IP (172.28.6.2) is stable — the daemon's DOCKER_HOST targets it
  # and dind's regenerated TLS server cert carries it, so --tlsverify holds across
  # recreates. Without a fixed subnet, docker draws this IP from its default pool and
  # a recreate can reassign it, breaking the cert's hostname match.
  dev-engine:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 172.28.6.0/24
  # Job containers ↔ the egress proxy. Job containers are attached to a per-job
  # network by the daemon's clamp; this network exists so the proxy has a stable,
  # pinned address that those per-job networks can be bridged to.
  dev-egress:
    driver: bridge
    internal: true
    ipam:
      config:
        - subnet: 172.28.5.0/24
  # The proxy's real path to the internet — deliberately NOT internal, and
  # deliberately NOT `omadia` (see the service comment above). No pinned
  # subnet/address: dev-egress-proxy is this network's only member, and
  # nothing else ever needs to address it here.
  dev-egress-external:
    driver: bridge
